version: 2
jobs:
  build:
    working_directory: ~/circletest
    machine: true
    steps:
      - checkout
      - run:
        name: docker-compose up
        command: |
          docker-compose -f env/local/docker/docker-compose.test.yml up -d